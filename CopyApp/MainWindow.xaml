<Window x:Class="CopyApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CopyApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    
    <DockPanel>
        <Grid DockPanel.Dock="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label FontSize="25">From</Label>
            <TextBox Grid.Column="1" 
                     Margin="5" 
                     Padding="10"
                     Name="src"></TextBox>
            <Button Grid.Column="2" 
                    Padding="5" 
                    Margin="3" 
                    FontSize="20"
                    Click="SourceButton_Click">Browse</Button>
            <Label Grid.Row="1" 
                   FontSize="25">To</Label>
            <TextBox Grid.Column="1" 
                     Grid.Row="1" 
                     Margin="5" 
                     Padding="10"
                     Name="dest"></TextBox>
            <Button Grid.Column="2" 
                    Grid.Row="1" 
                    Padding="5" 
                    Margin="3" 
                    FontSize="20"
                    Click="ToButton_Click_1">Browse</Button>
            <Button Grid.Row="2" 
                    Grid.ColumnSpan="3" 
                    FontSize="25" 
                    Margin="10"
                    Click="CopyButton_Click">Copy</Button>

        </Grid>

        <ProgressBar IsIndeterminate="True" DockPanel.Dock="Bottom" Height="10"></ProgressBar>
        <ListBox></ListBox>
    </DockPanel>
</Window>
